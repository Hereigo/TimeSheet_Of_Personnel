@model IEnumerable<TimeSheet_Of_Personnel.Models.CalendRecord>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DayType.DayTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.EmployeeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CalendRecordName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DayType.DayTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.EmployeeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CalendRecordName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CalendRecordID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CalendRecordID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CalendRecordID })
        </td>
    </tr>
}

</table>
