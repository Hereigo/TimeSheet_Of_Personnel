@model IEnumerable<TimeSheet_Of_Personnel.Models.DayType>

@{
    ViewBag.Title = "Index";
}

<br />
<h4>Перелік визначень днів, що використовуються у табелі:</h4>

<table class="table">
    <tr>
        <th></th>
        <th></th>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.DayTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SymbolName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SymbolNameLatin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkHours)
        </th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td></td>
        <td></td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id = item.DayTypeID }, new { @class = "glyphicon glyphicon-pencil" })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DayTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SymbolName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SymbolNameLatin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkHours)
        </td>
        <td></td>
        <td></td>
    </tr>
}

</table>

<p>
    @Html.ActionLink(" + Додати нове визначення", "Create", new { }, new { @class = "btn btn-default" })
</p>